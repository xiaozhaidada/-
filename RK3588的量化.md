## RK3588的量化

### 1.PTR（训练后量化）

```python
###### rknn 配置
# 默认
ret = rknn.config(
    target_platform='rk3588'
)

# 根据 dataset 修改
rknn.config(
    mean_values=[[x, y, z]],
    std_values=[[x, y, z]],
    target_platform='rk3588'
)
######

##### rknn构建
# 默认
ret = rknn.build(
    do_quantization=False
)

# PTR 量化
ret = rknn.build(
    do_quantization=True,
    dataset='./dataset.txt'
)
#####
```

